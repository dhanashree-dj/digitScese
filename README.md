# 🔢 DigitScese — Handwritten Digit Recognition (CNN)

DigitScese is a deep learning project that recognizes handwritten digits (0–9) using a Convolutional Neural Network (CNN) trained on the MNIST dataset.  
It can also predict digits from custom user-provided images.

---

## 🚀 Features

- CNN-based digit recognition
- Trained on MNIST dataset (60,000 images)
- ~99% test accuracy
- Custom image prediction support
- Automatic preprocessing (invert, crop, resize)
- Fast mode model save/load (no retraining every run)

---

## 🧠 Model Architecture

- Conv2D (32 filters)
- MaxPooling
- Conv2D (64 filters)
- MaxPooling
- Flatten
- Dense (128 neurons)
- Output Dense (10 classes — digits 0–9)

---

## 🛠️ Tech Stack

- Python
- TensorFlow / Keras
- NumPy
- Matplotlib
- Pillow (PIL)

---

## enjoy ur project --DJ

